[project]
name = "agent-server"
version = "0.1.1"
description = "The server that powers agent-tui"
readme = "README.md"
authors = [
    { name = "DavidKoleczek", email = "45405824+DavidKoleczek@users.noreply.github.com" },
]
requires-python = ">=3.13"
dependencies = [
    "SQLAlchemy>=2.0,<2.1",
    "anthropic>=0.115,<1.0",
    "crawl4ai[pdf]>=0.9,<1.0",
    "fastapi[standard]>=0.139,<1.0",
    "feedparser>=6.0,<7.0",
    "google-genai>=2.10,<3.0",
    "httpx>=0.28,<1.0",
    "interop-router==0.2.0",
    "jiter>=0.16,<1.0",
    "loguru>=0.7,<1.0",
    "markitdown==0.1.6",
    "openai>=2.44,<3.0",
    "pydantic>=2.13,<3.0",
    "pymupdf4llm>=1.28,<2.0",
    "pymupdf>=1.28,<2.0",
    "python-liquid>=2.2,<3.0",
    "tree-sitter-bash>=0.25,<1.0",
    "tree-sitter>=0.26,<1.0",
    "tzdata>=2026.2",
    "websockets>=16.0,<17.0",
]

[project.scripts]
agent-server = "agent_server.cli:run"

[dependency-groups]
dev = [
    "pytest-asyncio>=1.4",
    "pytest>=9.1",
    "ruff>=0.15",
    "ty>=0.0.35",
]

[build-system]
requires = ["uv_build>=0.11,<0.12.0"]
build-backend = "uv_build"

[tool.uv_build]
data = [
    { sources = [
        "src/agent_server/core/tools/_ripgrep/LICENSE-ripgrep",
    ], target = "agent_server/core/tools/_ripgrep" },
    { sources = [
        "src/agent_server/core/tools/_ripgrep/bin",
    ], target = "agent_server/core/tools/_ripgrep/bin" },
]

[tool.uv]
exclude-newer = "3 days"
exclude-newer-package = { interop-router = "2099-01-01" }

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = [
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "E",   # pycodestyle
    "F",   # pyflakes
    "I",   # isort
    "ISC", # flake8-implicit-str-concat
    "N",   # pep8-naming
    "PIE", # flake8-pie
    "PT",  # flake-pytest-style
    "PTH", # flake8-use-pathlib
    "RUF", # ruff
    "SIM", # flake8-simplify
    "TID", # flake8-tidy-imports
    "UP",  # pyupgrade
]
ignore = [
    "E501", # line-too-long (formatter makes best-effort attempt)
    # The following rules conflict with the formatter, see: https://docs.astral.sh/ruff/formatter/#conflicting-lint-rules
    "E111",   # indentation-with-invalid-multiple
    "E114",   # indentation-with-invalid-multiple-comment
    "E117",   # over-indented
    "ISC002", # multi-line-implicit-string-concatenation
    "W191",   # tab-indentation
]

[tool.ruff.lint.isort]
force-sort-within-sections = true
split-on-trailing-comma = false

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
